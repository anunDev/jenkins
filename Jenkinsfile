
pipeline {
   agent { label 'ecs-anun-dev' }
    stages {
        stage('Welcome Step') {
            steps { 
                echo 'running e2e test!'
                echo 'downloading anun-tracer'
            }
        }
    }
}
